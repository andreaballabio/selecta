import { HomeRedesign } from '@/components/marketing/home-redesign'

// Anteprima della homepage redesign con un set di brani d'esempio
// (la home reale "/" passa i brani pubblicati reali).
export default function PreviewPage() {
  return <HomeRedesign />
}
